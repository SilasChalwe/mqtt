// Force compilation of the BestFirstSearch implementation from the local library.
// This keeps the library files untouched while ensuring the linker sees the definitions.

#include "../lib/src/BestFirstSearch.cpp"
